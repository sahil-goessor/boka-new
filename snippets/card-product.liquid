
{{ 'component-rating.css' | asset_url | stylesheet_tag }}

{%- if card_product and card_product != empty -%}
  {%- liquid
    assign ratio = 1

    if card_product.featured_media and media_aspect_ratio == 'portrait'
      assign ratio = 0.8
    elsif card_product.featured_media and media_aspect_ratio == 'adapt'
      assign ratio = card_product.featured_media.aspect_ratio
    endif

    if ratio == 0 or ratio == null
      assign ratio = 1
    endif

    assign product_form_id = 'boka-card-ajax-add-' | append: section_id | append: '-' | append: card_product.id
    assign current_variant = card_product.selected_or_first_available_variant
  -%}

  <div class="card-wrapper product-card-wrapper boka-product-card underline-links-hover{% if template.suffix == 'subscription' %} product-{{ card_product.id }}{% endif %}">
    <div
      class="
        card
        card--{{ settings.card_style }}
        {% if card_product.featured_media %} card--media{% else %} card--text{% endif %}
        {% if settings.card_style == 'card' %} color-{{ settings.card_color_scheme }} gradient{% endif %}
        {% if extend_height %} card--extend-height{% endif %}
        {% if card_product.featured_media == nil and settings.card_style == 'card' %} ratio{% endif %}
        {% if horizontal_class %} card--horizontal{% endif %}
      "
      style="--ratio-percent: {{ 1 | divided_by: ratio | times: 100 }}%;"
    >
      <div
        class="card__inner {% if settings.card_style == 'standard' %}color-{{ settings.card_color_scheme }} gradient{% endif %}{% if card_product.featured_media or settings.card_style == 'standard' %} ratio{% endif %}"
        style="--ratio-percent: {{ 1 | divided_by: ratio | times: 100 }}%;"
      >
        {%- if card_product.featured_media -%}
          <a
            href="{% if settings.theme_include_collection_url %}{{ card_product.url | within: collection }}{% else %}{{ card_product.url }}{% endif %}"
            class="card__media-link"
            aria-label="{{ card_product.title | escape }}"
          >
            <div class="card__media">
              <div class="media media--transparent media--hover-effect">
                {% comment %}theme-check-disable ImgLazyLoading{% endcomment %}
                <img
                  srcset="
                    {%- if card_product.featured_media.width >= 165 -%}{{ card_product.featured_media | image_url: width: 165 }} 165w,{%- endif -%}
                    {%- if card_product.featured_media.width >= 360 -%}{{ card_product.featured_media | image_url: width: 360 }} 360w,{%- endif -%}
                    {%- if card_product.featured_media.width >= 533 -%}{{ card_product.featured_media | image_url: width: 533 }} 533w,{%- endif -%}
                    {%- if card_product.featured_media.width >= 720 -%}{{ card_product.featured_media | image_url: width: 720 }} 720w,{%- endif -%}
                    {%- if card_product.featured_media.width >= 940 -%}{{ card_product.featured_media | image_url: width: 940 }} 940w,{%- endif -%}
                    {%- if card_product.featured_media.width >= 1066 -%}{{ card_product.featured_media | image_url: width: 1066 }} 1066w,{%- endif -%}
                    {{ card_product.featured_media | image_url }} {{ card_product.featured_media.width }}w
                  "
                  src="{{ card_product.featured_media | image_url: width: 533 }}"
                  sizes="(min-width: {{ settings.page_width }}px) {{ settings.page_width | minus: 130 | divided_by: 4 }}px, (min-width: 990px) calc((100vw - 130px) / 4), (min-width: 750px) calc((100vw - 120px) / 3), calc((100vw - 44px) / 2)"
                  alt="{{ card_product.featured_media.alt | escape }}"
                  class="motion-reduce"
                  {% unless lazy_load == false %}
                    loading="lazy"
                  {% endunless %}
                  width="{{ card_product.featured_media.width }}"
                  height="{{ card_product.featured_media.height }}"
                >
                {% comment %}theme-check-enable ImgLazyLoading{% endcomment %}

                {%- if card_product.media[1] != null and show_secondary_image -%}
                  <img
                    srcset="
                      {%- if card_product.media[1].width >= 165 -%}{{ card_product.media[1] | image_url: width: 165 }} 165w,{%- endif -%}
                      {%- if card_product.media[1].width >= 360 -%}{{ card_product.media[1] | image_url: width: 360 }} 360w,{%- endif -%}
                      {%- if card_product.media[1].width >= 533 -%}{{ card_product.media[1] | image_url: width: 533 }} 533w,{%- endif -%}
                      {%- if card_product.media[1].width >= 720 -%}{{ card_product.media[1] | image_url: width: 720 }} 720w,{%- endif -%}
                      {%- if card_product.media[1].width >= 940 -%}{{ card_product.media[1] | image_url: width: 940 }} 940w,{%- endif -%}
                      {%- if card_product.media[1].width >= 1066 -%}{{ card_product.media[1] | image_url: width: 1066 }} 1066w,{%- endif -%}
                      {{ card_product.media[1] | image_url }} {{ card_product.media[1].width }}w
                    "
                    src="{{ card_product.media[1] | image_url: width: 533 }}"
                    sizes="(min-width: {{ settings.page_width }}px) {{ settings.page_width | minus: 130 | divided_by: 4 }}px, (min-width: 990px) calc((100vw - 130px) / 4), (min-width: 750px) calc((100vw - 120px) / 3), calc((100vw - 44px) / 2)"
                    alt=""
                    class="motion-reduce"
                    loading="lazy"
                    width="{{ card_product.media[1].width }}"
                    height="{{ card_product.media[1].height }}"
                  >
                {%- endif -%}
              </div>
            </div>
          </a>
        {%- endif -%}

        {% render 'custom-badge', product: card_product %}

        <div class="card__badge {{ settings.badge_position }}">
          {%- if card_product.available == false -%}
            <span
              id="Badge-{{ section_id }}-{{ card_product.id }}"
              class="badge badge--bottom-left color-{{ settings.sold_out_badge_color_scheme }}"
            >
              {{- 'products.product.sold_out' | t -}}
            </span>
          {%- elsif card_product.compare_at_price > card_product.price and card_product.available -%}
            <span
              id="Badge-{{ section_id }}-{{ card_product.id }}"
              class="badge badge--bottom-left color-{{ settings.sale_badge_color_scheme }}"
            >
              {{- 'products.product.on_sale' | t -}}
            </span>
          {%- endif -%}
        </div>

        <div class="card__content small-hide medium-hide">
          <div class="cta-buttons buttons-desktop">
            {% if template.suffix != 'subscription' %}
              {%- if enable_learn_more -%}
                <div class="learn-more">
                  <a
                    aria-label="Learn more about {{ card_product.title | escape }}"
                    class="button button--full-width button--secondary"
                    href="{% if settings.theme_include_collection_url %}{{ card_product.url | within: collection }}{% else %}{{ card_product.url }}{% endif %}"
                  >
                    Learn More
                    <span class="visually-hidden"> about {{ card_product.title | escape }}</span>
                  </a>
                </div>
              {%- endif -%}
            {% endif %}

            {%- if show_quick_add -%}
              <div class="quick-add no-js-hidden">
                {% if template.suffix != 'subscription' %}
                  <form
                    method="post"
                    action="{{ routes.cart_add_url }}"
                    id="{{ product_form_id }}-desktop"
                    class="form boka-card-ajax-form"
                    data-type="add-to-cart-form"
                    data-boka-ajax-add
                  >
                    <input type="hidden" name="id" value="{{ current_variant.id }}">
                    <input type="hidden" name="quantity" value="1">

                    <button
                      id="{{ product_form_id }}-desktop-submit"
                      type="submit"
                      name="add"
                      class="quick-add__submit button button--full-width button--primary{% if horizontal_quick_add %} card--horizontal__quick-add{% endif %}"
                      aria-label="Add {{ card_product.title | escape }} to cart"
                      aria-live="polite"
                      data-sold-out-message="true"
                      {% if current_variant.available == false %}
                        disabled
                      {% endif %}
                    >
                      <span data-boka-button-text>
                        {%- if current_variant.available -%}
                          Add to cart
                        {%- else -%}
                          {{ 'products.product.sold_out' | t }}
                        {%- endif -%}
                      </span>

                      <span class="sold-out-message hidden">
                        {{ 'products.product.sold_out' | t }}
                      </span>

                      {%- if horizontal_quick_add -%}
                        <span class="icon-wrap">{% render 'icon-plus' %}</span>
                      {%- endif -%}

                      <div class="loading-overlay__spinner hidden">
                        <svg
                          aria-hidden="true"
                          focusable="false"
                          class="spinner"
                          viewBox="0 0 66 66"
                          xmlns="http://www.w3.org/2000/svg"
                        >
                          <circle class="path" fill="none" stroke-width="6" cx="33" cy="33" r="30"></circle>
                        </svg>
                      </div>
                    </button>
                  </form>
                {% else %}
                  <button
                    id="{{ card_product.id }}-desktop-submit"
                    type="button"
                    name="add"
                    class="add-to-cart-dummy button button--full-width button--primary{% if horizontal_quick_add %} card--horizontal__quick-add{% endif %}"
                    aria-label="Add {{ card_product.title | escape }} to cart"
                    aria-live="polite"
                    data-sold-out-message="true"
                    {% if current_variant.available == false %}
                      disabled
                    {% endif %}
                  >
                    <span>
                      {%- if current_variant.available -%}
                        Add to cart
                      {%- else -%}
                        {{ 'products.product.sold_out' | t }}
                      {%- endif -%}
                    </span>
                    <span class="sold-out-message hidden">
                      {{ 'products.product.sold_out' | t }}
                    </span>
                  </button>
                {% endif %}
              </div>
            {%- endif -%}
          </div>
        </div>
      </div>

      <div class="card__content">
        <div class="card__information">
          <h2
            class="card__heading{% if card_product.featured_media or settings.card_style == 'standard' %} h5{% endif %}"
            {% if card_product.featured_media or settings.card_style == 'card' %}
              id="title-{{ section_id }}-{{ card_product.id }}"
            {% endif %}
          >
            <a
              href="{% if settings.theme_include_collection_url %}{{ card_product.url | within: collection }}{% else %}{{ card_product.url }}{% endif %}"
              id="CardLink-{{ section_id }}-{{ card_product.id }}"
              class="full-unstyled-link"
            >
              {{ card_product.title | escape }}
            </a>
          </h2>

          <div
            class="yotpo-widget-instance"
            data-yotpo-instance-id="1019166"
            data-yotpo-product-id="{{ card_product.id }}"
            data-yotpo-section-id="collection"
          ></div>

          <script>
            if (!window.bokaYotpoInitScriptLoaded) {
              window.bokaYotpoInitScriptLoaded = true;

              function initYotpoWidgets() {
                if (
                  typeof window.yotpoWidgetsContainer !== 'undefined' &&
                  window.yotpoWidgetsContainer &&
                  Array.isArray(window.yotpoWidgetsContainer.yotpo_widget_initializer) &&
                  window.yotpoWidgetsContainer.yotpo_widget_initializer.length >= 1 &&
                  typeof window.yotpoWidgetsContainer.initWidgets === 'function'
                ) {
                  window.yotpoWidgetsContainer.initWidgets(false);
                  return;
                }

                window.bokaYotpoInitAttempts = window.bokaYotpoInitAttempts || 0;

                if (window.bokaYotpoInitAttempts < 10) {
                  window.bokaYotpoInitAttempts += 1;
                  setTimeout(initYotpoWidgets, 500);
                }
              }

              initYotpoWidgets();
            }
          </script>

          <div class="card-information">
            {%- if show_vendor -%}
              <span class="visually-hidden">{{ 'accessibility.vendor' | t }}</span>
              <div class="caption-with-letter-spacing light">{{ card_product.vendor }}</div>
            {%- endif -%}

            <span class="caption-large light">{{ block.settings.description | escape }}</span>

            {%- if show_rating and card_product.metafields.reviews.rating.value != blank -%}
              {% liquid
                assign rating_decimal = 0
                assign decimal = card_product.metafields.reviews.rating.value.rating | modulo: 1
                if decimal >= 0.3 and decimal <= 0.7
                  assign rating_decimal = 0.5
                elsif decimal > 0.7
                  assign rating_decimal = 1
                endif
              %}
              <div
                class="rating"
                role="img"
                aria-label="{{ 'accessibility.star_reviews_info' | t: rating_value: card_product.metafields.reviews.rating.value, rating_max: card_product.metafields.reviews.rating.value.scale_max }}"
              >
                <span
                  aria-hidden="true"
                  class="rating-star color-icon-{{ settings.accent_icons }}"
                  style="--rating: {{ card_product.metafields.reviews.rating.value.rating | floor }}; --rating-max: {{ card_product.metafields.reviews.rating.value.scale_max }}; --rating-decimal: {{ rating_decimal }};"
                ></span>
              </div>
              <p class="rating-text caption">
                <span aria-hidden="true">
                  {{- card_product.metafields.reviews.rating.value }} /
                  {{ card_product.metafields.reviews.rating.value.scale_max -}}
                </span>
              </p>
              <p class="rating-count caption">
                <span aria-hidden="true">({{ card_product.metafields.reviews.rating_count }})</span>
                <span class="visually-hidden">
                  {{- card_product.metafields.reviews.rating_count }}
                  {{ 'accessibility.total_reviews' | t -}}
                </span>
              </p>
            {%- endif -%}

            {% render 'price', product: card_product, price_class: '', is_product_block: is_complementary_block %}
          </div>
        </div>

        <div class="cta-buttons buttons-mobile large-up-hide">
          {%- if show_quick_add -%}
            <div class="quick-add no-js-hidden">
              {% if template.suffix != 'subscription' %}
                <form
                  method="post"
                  action="{{ routes.cart_add_url }}"
                  id="{{ product_form_id }}-mobile"
                  class="form boka-card-ajax-form"
                  data-type="add-to-cart-form"
                  data-boka-ajax-add
                >
                  <input type="hidden" name="id" value="{{ current_variant.id }}">
                  <input type="hidden" name="quantity" value="1">

                  <button
                    id="{{ product_form_id }}-mobile-submit"
                    type="submit"
                    name="add"
                    class="quick-add__submit button button--full-width button--primary{% if horizontal_quick_add %} card--horizontal__quick-add{% endif %}"
                    aria-label="Add {{ card_product.title | escape }} to cart"
                    aria-live="polite"
                    data-sold-out-message="true"
                    {% if current_variant.available == false %}
                      disabled
                    {% endif %}
                  >
                    <span data-boka-button-text>
                      {%- if current_variant.available -%}
                        Add to cart
                      {%- else -%}
                        {{ 'products.product.sold_out' | t }}
                      {%- endif -%}
                    </span>

                    <span class="sold-out-message hidden">
                      {{ 'products.product.sold_out' | t }}
                    </span>

                    {%- if horizontal_quick_add -%}
                      <span class="icon-wrap">{% render 'icon-plus' %}</span>
                    {%- endif -%}

                    <div class="loading-overlay__spinner hidden">
                      <svg
                        aria-hidden="true"
                        focusable="false"
                        class="spinner"
                        viewBox="0 0 66 66"
                        xmlns="http://www.w3.org/2000/svg"
                      >
                        <circle class="path" fill="none" stroke-width="6" cx="33" cy="33" r="30"></circle>
                      </svg>
                    </div>
                  </button>
                </form>
              {% else %}
                <button
                  id="{{ card_product.id }}-mobile-submit"
                  type="button"
                  name="add"
                  class="add-to-cart-dummy button button--full-width button--primary{% if horizontal_quick_add %} card--horizontal__quick-add{% endif %}"
                  aria-label="Add {{ card_product.title | escape }} to cart"
                  aria-live="polite"
                  data-sold-out-message="true"
                  {% if current_variant.available == false %}
                    disabled
                  {% endif %}
                >
                  <span>
                    {%- if current_variant.available -%}
                      Add to cart
                    {%- else -%}
                      {{ 'products.product.sold_out' | t }}
                    {%- endif -%}
                  </span>
                  <span class="sold-out-message hidden">
                    {{ 'products.product.sold_out' | t }}
                  </span>
                </button>
              {% endif %}
            </div>
          {%- endif -%}

          {% if template.suffix != 'subscription' %}
            {%- if enable_learn_more -%}
              <div class="learn-more">
                <a
                  aria-label="View details for {{ card_product.title | escape }}"
                  class="button button--full-width button--secondary"
                  href="{% if settings.theme_include_collection_url %}{{ card_product.url | within: collection }}{% else %}{{ card_product.url }}{% endif %}"
                >
                  View details
                  <span class="visually-hidden"> for {{ card_product.title | escape }}</span>
                </a>
              </div>
            {%- endif -%}
          {% endif %}
        </div>

        {% if template.suffix == 'subscription' %}
          <div class="recharge-custom-widget" style="z-index: 2;">
            <product-form class="product-form">
              {% assign subscription_class = 'product-form-' | append: card_product.id %}
              {%- form 'product',
                card_product,
                id: subscription_class,
                class: 'form',
                novalidate: 'novalidate',
                data-type: 'add-to-cart-form',
                data-productid: card_product.id
              -%}
                <input
                  type="hidden"
                  name="id"
                  data-productid="{{ card_product.id }}"
                  value="{{ card_product.variants.first.id }}"
                >
                {% render 'subscription-collection', product: card_product %}
                <button
                  id="ProductSubmitButton-{{ card_product.id }}"
                  type="submit"
                  name="add"
                  class="subscribe-button button button--full-width button--primary hidden"
                  {% if card_product.selected_or_first_available_variant.available == false or quantity_rule_soldout %}
                    disabled
                  {% endif %}
                >
                  <span>
                    {%- if card_product.selected_or_first_available_variant.available == false or quantity_rule_soldout -%}
                      {{ 'products.product.sold_out' | t }}
                    {%- else -%}
                      {{ 'products.product.add_to_cart' | t }}
                    {%- endif -%}
                  </span>
                  <div class="loading-overlay__spinner hidden">
                    <svg
                      aria-hidden="true"
                      focusable="false"
                      class="spinner"
                      viewBox="0 0 66 66"
                      xmlns="http://www.w3.org/2000/svg"
                    >
                      <circle class="path" fill="none" stroke-width="6" cx="33" cy="33" r="30"></circle>
                    </svg>
                  </div>
                </button>
              {%- endform -%}
            </product-form>
          </div>
        {% endif %}
      </div>
    </div>
  </div>
{%- else -%}
  <div class="product-card-wrapper card-wrapper boka-product-card underline-links-hover">
    <div
      class="
        card
        card--{{ settings.card_style }}
        card--text
        {% if extend_height %} card--extend-height{% endif %}
        {% if settings.card_style == 'card' %} color-{{ settings.card_color_scheme }} gradient{% endif %}
        {% if settings.card_style == 'card' %} ratio{% endif %}
        {{ horizontal_class }}
      "
      style="--ratio-percent: 100%;"
    >
      <div
        class="card__inner {% if settings.card_style == 'standard' %}color-{{ settings.card_color_scheme }} gradient{% endif %}{% if settings.card_style == 'standard' %} ratio{% endif %}"
        style="--ratio-percent: 100%;"
      >
        <div class="card__content">
          <div class="card__information">
            <h3 class="card__heading">
              <a role="link" aria-disabled="true" class="full-unstyled-link">
                {{ 'onboarding.product_title' | t }}
              </a>
            </h3>
          </div>
        </div>
      </div>

      <div class="card__content">
        <div class="card__information">
          <h3 class="card__heading{% if settings.card_style == 'standard' %} h5{% endif %}">
            <a role="link" aria-disabled="true" class="full-unstyled-link">
              {{ 'onboarding.product_title' | t }}
            </a>
          </h3>
          <div class="card-information">
            {%- if show_vendor -%}
              <span class="visually-hidden">{{ 'accessibility.vendor' | t }}</span>
              <div class="caption-with-letter-spacing light">{{ 'products.product.vendor' | t }}</div>
            {%- endif -%}
            {% render 'price' %}
          </div>
        </div>
      </div>
    </div>
  </div>
{%- endif -%}

<script>
  if (!window.bokaCollectionAjaxAddLoaded) {
    window.bokaCollectionAjaxAddLoaded = true;

    window.bokaCollectionAjaxAddToCart = function(form) {
      var submitButton = form.querySelector('[type="submit"]');
      var spinner = form.querySelector('.loading-overlay__spinner');
      var buttonText = form.querySelector('[data-boka-button-text]');
      var originalText = buttonText ? buttonText.textContent : '';

      if (!submitButton || submitButton.disabled) return;

      submitButton.setAttribute('aria-disabled', 'true');
      submitButton.classList.add('loading');

      if (spinner) spinner.classList.remove('hidden');
      if (buttonText) buttonText.textContent = 'Adding...';

      var formData = new FormData(form);
      var cart = document.querySelector('cart-notification') || document.querySelector('cart-drawer');

      if (cart && typeof cart.getSectionsToRender === 'function') {
        formData.append(
          'sections',
          cart.getSectionsToRender().map(function(section) {
            return section.id;
          })
        );
        formData.append('sections_url', window.location.pathname);
      }

      fetch(form.getAttribute('action') || (window.routes && window.routes.cart_add_url) || '/cart/add.js', {
        method: 'POST',
        headers: {
          'X-Requested-With': 'XMLHttpRequest',
          'Accept': 'application/javascript'
        },
        body: formData
      })
        .then(function(response) {
          return response.json();
        })
        .then(function(response) {
          if (response.status) {
            throw response;
          }

          if (cart && typeof cart.renderContents === 'function') {
            cart.renderContents(response);
          } else {
            document.dispatchEvent(
              new CustomEvent('cart:refresh', {
                bubbles: true,
                detail: response
              })
            );
            document.dispatchEvent(
              new CustomEvent('cart:updated', {
                bubbles: true,
                detail: response
              })
            );
          }

          if (document.querySelector('cart-drawer') && typeof document.querySelector('cart-drawer').open === 'function') {
            document.querySelector('cart-drawer').open();
          }

          if (buttonText) buttonText.textContent = 'Added';
          setTimeout(function() {
            if (buttonText) buttonText.textContent = originalText.trim() || 'Add to cart';
          }, 1400);
        })
        .catch(function(error) {
          console.error('Boka collection ajax add error:', error);
          if (buttonText) buttonText.textContent = error.description || 'Error';
          setTimeout(function() {
            if (buttonText) buttonText.textContent = originalText.trim() || 'Add to cart';
          }, 1800);
        })
        .finally(function() {
          submitButton.removeAttribute('aria-disabled');
          submitButton.classList.remove('loading');
          if (spinner) spinner.classList.add('hidden');
        });
    };

    document.addEventListener('submit', function(event) {
      var form = event.target.closest('[data-boka-ajax-add]');
      if (!form) return;

      event.preventDefault();
      window.bokaCollectionAjaxAddToCart(form);
    });
  }
</script>

<style>
  .price--on-sale .price-item--regular {
    font-size: 1.6rem;
  }

  .boka-product-card .boka-card-ajax-form {
    margin: 0;
  }

  .boka-product-card .card__media-link {
    display: block;
    height: 100%;
    text-decoration: none;
  }

  .boka-product-card .buttons-mobile {
    display: none;
  }

  @media screen and (max-width: 749px) {
    .boka-product-card.product-card-wrapper .card {
      background: transparent;
    }

    .boka-product-card.product-card-wrapper .card__inner {
      background: #f2f2ef;
      border-radius: 10px;
      overflow: hidden;
    }

    .boka-product-card.product-card-wrapper .card__media,
    .boka-product-card.product-card-wrapper .media {
      background: #f2f2ef;
      border-radius: 10px;
    }

    .boka-product-card.product-card-wrapper .card__media img,
    .boka-product-card.product-card-wrapper .media img {
      object-fit: contain;
      padding: 18px;
      transform: scale(0.92);
    }

    .boka-product-card.product-card-wrapper .card__badge {
      top: 18px;
      left: 18px;
      right: auto;
      bottom: auto;
      z-index: 2;
    }

    .boka-product-card.product-card-wrapper .badge {
      border: 0;
      border-radius: 999px;
      background: #ffffff;
      color: #222721;
      font-size: 12px;
      font-weight: 700;
      letter-spacing: 0.04em;
      line-height: 1;
      padding: 9px 18px;
      text-transform: uppercase;
    }

    .boka-product-card.product-card-wrapper .card__content {
      padding: 0;
    }

    .boka-product-card.product-card-wrapper > .card,
    .boka-product-card.product-card-wrapper .card {
      height: 100%;
    }

    .boka-product-card.product-card-wrapper .card > .card__content {
      display: flex;
      flex-direction: column;
      flex-grow: 1;
    }

    .boka-product-card.product-card-wrapper .card__information {
      padding: 18px 0 0;
    }

    .boka-product-card.product-card-wrapper .card__heading {
      margin: 0 0 14px;
      color: #1f2721;
      font-size: 20px;
      line-height: 1;
      font-weight: 700;
      letter-spacing: -0.045em;
    }

    .boka-product-card.product-card-wrapper .card__heading a {
      color: inherit;
      text-decoration: none;
    }

    .boka-product-card.product-card-wrapper .yotpo-widget-instance {
      margin: 0 0 12px;
      min-height: 22px;
    }

    .boka-product-card.product-card-wrapper .card-information {
      margin: 0;
    }

    .boka-product-card.product-card-wrapper .price {
      margin: 0 0 18px;
      color: #1f2721;
      font-size: 20px;
      line-height: 1.1;
      font-weight: 700;
      letter-spacing: -0.03em;
    }

    .boka-product-card.product-card-wrapper .price--on-sale .price-item--regular {
      font-size: 1.4rem;
    }

    .boka-product-card.product-card-wrapper .buttons-mobile {
      display: flex;
      flex-direction: column;
      gap: 14px;
      margin-top: auto;
    }

    .boka-product-card.product-card-wrapper .buttons-mobile .quick-add,
    .boka-product-card.product-card-wrapper .buttons-mobile .learn-more {
      margin: 0;
    }

    .boka-product-card.product-card-wrapper .buttons-mobile .button {
      min-height: 64px;
      width: 100%;
      border-radius: 999px;
      font-size: 17px;
      font-weight: 700;
      letter-spacing: -0.03em;
      line-height: 1;
      text-transform: none;
      box-shadow: none;
    }

    .boka-product-card.product-card-wrapper .buttons-mobile .button:before,
    .boka-product-card.product-card-wrapper .buttons-mobile .button:after {
      display: none;
    }

    .boka-product-card.product-card-wrapper .buttons-mobile .button--primary {
      background: #1f2721;
      border: 1px solid #1f2721;
      color: #ffffff;
    }

    .boka-product-card.product-card-wrapper .buttons-mobile .button--secondary {
      background: transparent;
      border: 1px solid rgba(31, 39, 33, 0.22);
      color: #1f2721;
    }

    .boka-product-card.product-card-wrapper .buttons-mobile .button.loading {
      opacity: 0.78;
      pointer-events: none;
    }
  }

  @media screen and (max-width: 389px) {
    .boka-product-card.product-card-wrapper .card__heading {
      font-size: 18px;
    }

    .boka-product-card.product-card-wrapper .price {
      font-size: 18px;
    }

    .boka-product-card.product-card-wrapper .buttons-mobile .button {
      min-height: 58px;
      font-size: 16px;
    }
  }
</style>
